# Docker Volume Configuration
# Copy this file to volumes.env and customize the paths for your machine
# If not set, volumes will default to subdirectories in the project

# Fast storage (SSD/NVMe) - for checkpoints, models, active data
FAST_STORAGE=/path/to/fast/storage
# Example: FAST_STORAGE=/mnt/nvme/ts-dia

# Slow storage (HDD/Network) - for large datasets, archives
SLOW_STORAGE=/path/to/slow/storage
# Example: SLOW_STORAGE=/mnt/hdd/datasets

# Temp storage (scratch space) - for temporary files, cache
TEMP_STORAGE=/path/to/temp/storage
# Example: TEMP_STORAGE=/tmp/ts-dia

# Note: If these are not set, the following defaults will be used:
# FAST_STORAGE -> ./storage/fast
# SLOW_STORAGE -> ./storage/slow
# TEMP_STORAGE -> ./storage/temp

